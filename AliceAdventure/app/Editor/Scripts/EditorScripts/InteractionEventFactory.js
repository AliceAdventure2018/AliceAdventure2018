'use strict';

var InteractionEvent = require('./InteractionEvent');
var Type = require('./Type');

var EventFactory;

EventFactory = function(_id, _name，_type, obj1, obj2,stat){

	var event;
	if(type === Type.Switch ){
		event = new InteractionEvent()
	}
}